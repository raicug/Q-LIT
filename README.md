# Q-LIT
Q-LIT is a lightweight, header-only C++ utility that obfuscates string literals at compile time using XOR encoding and decrypts them lazily at runtime, keeping plaintext strings out of compiled binaries.
